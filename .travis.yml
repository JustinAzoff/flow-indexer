language: go

go:
  - "1.10"
  - "1.11"
  - tip

cache:
  directories:
  - $HOME/.sources

addons:
  apt:
    packages:
      - flex
      - bison
      - libbz2-dev
      - liblzo2-dev
      - argus-client

before_install:
  - ./.ci/install-nfdump.sh

before_script:
  - export PATH=$PATH:$HOME/local/bin
